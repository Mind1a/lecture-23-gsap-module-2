# GSAP `.to()`

## What is `gsap.to()`?

`gsap.to()` says: "Take this thing and MOVE IT TO a new place!"

---

## 🎯 What Each Property Does:

### **`x`** - Move LEFT or RIGHT

- `x: 100` → Move 100 pixels to the RIGHT ➡️
- `x: -100` → Move 100 pixels to the LEFT ⬅️

### **`y`** - Move UP or DOWN

- `y: 100` → Move 100 pixels DOWN ⬇️
- `y: -100` → Move 100 pixels UP ⬆️

### **`scale`** - Make BIGGER or SMALLER

- `scale: 2` → Make 2 times BIGGER 📈
- `scale: 0.5` → Make half the size (smaller) 📉

### **`rotation`** - SPIN

- `rotation: 360` → Spin one full circle 🔄
- `rotation: 180` → Spin halfway

### **`opacity`** - How SEE-THROUGH it is

- `opacity: 1` → Fully visible 👁️
- `opacity: 0` → Invisible 👻
- `opacity: 0.5` → Half visible (ghostly)

### **`duration`** - How LONG it takes

- `duration: 1` → Takes 1 second ⏱️
- `duration: 2` → Takes 2 seconds (slower)
- `duration: 0.5` → Takes half a second (faster!)

---

## 📝 The Basic Recipe:

```javascript
gsap.to(".my-box", {
  x: 200, // WHERE to go
  duration: 1, // HOW LONG it takes
});
```

That's it! Just TWO things:

1. **What to animate** (`.my-box`)
2. **What to change** (`x: 200` and how long)
